# Frontend — copia a .env.local si lo necesitas.

# URL base del backend. Déjala VACÍA en desarrollo (Vite hace proxy a
# localhost:3000). En producción (Vercel), ponla con la URL pública del
# backend en Railway, p. ej.: https://contractlens-backend.up.railway.app
VITE_API_BASE=
